tpa-enabled: true
keep-inventory: true

# Add one small, fully enclosed diamond vein to a balanced set of normal-world
# chunks. The 4x4 chunk balancing reduces long dry runs while preserving every
# vein selected by the earlier 25% algorithm. Versioned markers prevent repeats.
diamond-bonus:
  enabled: true
  rate: 0.35
  profile-version: 2
