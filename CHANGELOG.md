# 개발 변경 기록

## 2026-09-06

- 손에 든 광원의 동적 조명을 아이템과 엔티티에도 적용한다. 카메라 기준 정점 좌표를 월드 좌표로 바꿔 거리에 따라 밝기를 줄이며, 하늘빛과 인벤토리 미리보기 조명은 유지한다.

- 배포별 제목과 설명을 게임 채팅에 두 줄로 표시한다. 운영 프론트엔드 빌드는 안내의 버전이 배포 커밋 수와 다르거나 필수 문구가 비어 있으면 실패한다.

- 첫 접속의 26.2 이전 안내 화면과 확인 버튼을 제거해 포털을 바로 표시한다.

- 음식 HUD를 운영 빌드에 포함한다. 포화도·소모도, 음식 회복량 미리보기, 인벤토리 음식 툴팁과 예상 체력 회복을 표시하며, 음식 WASM 패치를 CDN 소스 해시 검증에 포함한다.

- 변경 내역의 각 항목 앞에 `•`를 붙여 목록으로 표시한다.

- Paper 26.2 플러그인의 폐기 예정 API를 현재 채팅·접속·게임 규칙·아이템·프로필 차단 API로 바꾸고, 쓰지 않는 1.12 호환 코드를 제거했다. 채팅 기록과 관전자 숨김을 유지하며, 현대 플러그인 빌드는 폐기 예정 API 경고가 생기면 실패한다.

- 로고 옆 버전을 커밋 해시에서 누적 커밋 수로 바꿨다. 100번째 커밋은 `v100`으로 표시하며, Railway도 배포 커밋까지의 전체 기록으로 계산한다. 기록이 부족하면 잘못된 번호로 배포하지 않고 빌드를 중단한다.

- 26.2 메인·청크 작업용 WASM의 수학 함수 호출 2,844곳을 같은 결과를 내는 WASM 명령으로 바꾸고, 지형 렌더 루프의 읽기 전용 레이어 배열을 재사용한다. 해시·명령 위치·수학 결과·컴파일 검증을 통과했다. 실제 FPS 개선 폭은 측정하지 않았다.

- 26.2 로컬 빌드에 포화도·소모도, 음식 툴팁과 예상 체력 회복 HUD를 추가했다. 서버 음식 상태 코드를 분리하고, 게임 캔버스가 없을 때 자기 캔버스를 기준으로 잡던 오류를 고쳤다. 운영 런처에서는 실험 스크립트를 불러오지 않으며, WASM 검증은 임시 파일에서 실행한다.

- 포털 로고를 갈무리 글자로 바꾸고 배포 커밋 해시를 버전으로 표시한다. 헤더 아래 간격을 8px로 줄이고 로고를 4px 안쪽으로 옮겼다. 쓰지 않는 SVG 로고 코드를 제거하고 Git 정보가 없는 개발 빌드도 테스트에서 허용했다.

- 26.2 게임의 패치된 WASM과 리소스를 Cloudflare Pages에서 제공하도록 옮겼다. 포털에서 미리 다운로드·컴파일한 결과를 게임 프레임이 재사용하며, 브라우저 캐시가 없어도 리소스를 다시 받지 않는다. 별도 Brotli 디코더 대신 브라우저 압축 해제를 사용하고 빌드 압축을 강화해 메인 WASM 전송량을 약 20.5% 줄였다. 로그인·게임 연결·Paper 서버와 월드 저장소는 Railway에 유지한다. Railway 빌드는 게시된 완성본을 사용하고 대용량 게임 원본을 다시 받지 않으며, 소스 해시를 검사해 파일 배포 누락을 막는다.

- 입력창과 선택 입력을 44px 높이로 통일하고, 모바일에서만 커지거나 작아지던 버튼·탭 크기 규칙을 제거했다. 관리자 화면의 열 배치와 스크롤 같은 반응형 레이아웃은 유지한다.

- 운영에 연결되지 않은 Complementary 셰이더 실험 코드, Prism 소스 추출 도구와 미완성 브라우저 소스 후보 패치를 제거했다. 검토 결과는 문서에 남기고, 26.2 실행·렌더링·이전 도구와 비교 클라이언트는 유지했다.
- 26.2 런처 생성 코드의 중복 패치 검사를 한 함수로 모으고 빌드 준비 코드의 중복 import를 정리했다. 원본 해시와 패치 위치 검사는 유지한다.

- 청크 묶음 렌더링, 정점 상태 캐시, Binaryen WASM 재최적화를 실제 브라우저에서 검토했다. CPU 개선이 뚜렷하지 않거나 다운로드가 커져 운영에는 적용하지 않았다. 공개 브라우저 소스 후보의 GPU 구현이 빈 껍데기임을 확인하고 소스 이식 상태 설명을 바로잡았다.

- Complementary Reimagined 5.7.1 LOW의 원본 지형·그림자 코드를 WebGL2로 변환하는 독립 실험을 추가했다. 브라우저에서 4개 셰이더 컴파일과 합성 지형의 그림자 버퍼 영향을 확인했다. 운영 게임 연결, 최종 합성 화질, FPS 검증은 포함하지 않는다.

- 26.2 미리보기의 세로 화면 안내, 시작·오류 화면과 Eaglercraft 전용 게임 규칙·터치 설정 문구를 한국어로 바꿨다. 기본 한국어 파일에서 빠진 게임 문구도 보완했다.

- 26.2 미리보기 화면의 글자를 브라우저에서 더 매끄럽게 표시하도록 글꼴 스무딩을 적용했다.

- 가까운 곳에 거친 지형이 겹쳐 보이던 간이 LOD를 제거했다. 지형 조회 API와 서버 표면 샘플링도 없애며, 서버 렌더 거리는 늘리지 않는다.

- Prism의 실제 Java 26.2와 설치된 FO 모드에서 렌더링 소스를 추출하는 개발 도구를 추가했다. 26.2 브라우저 플랫폼 후보의 Java 컴파일을 확인했으며, 운영 클라이언트 교체나 FO 이식 완료를 뜻하지 않는다.

- 브라우저 렌더링 상태 캐시로 같은 GPU 설정의 중복 호출을 생략한다. 로컬 정지 장면에서 해당 호출의 약 67%를 줄였으며, FO 전체 이식이나 FPS 상승을 의미하지 않는다.

- 플레이어 채팅의 머리 아이콘, 이름, 본문에 적용되던 글자 그림자를 제거했다.

- 26.2 게임의 고정 60·120 FPS 제한을 실행 시 해제하고 VSync를 켠다. Minecraft의 무제한 설정을 사용해 브라우저가 제공하는 화면 주사율을 따르며, 120Hz보다 높은 모니터도 별도 상한으로 막지 않는다.

- 채팅을 `머리 아이콘 굵은 이름 내용`으로 바꾸고 이름의 꺾쇠를 제거했다. 실제 Galmuri11 Bold 2.40.3 글리프를 이름에만 사용하며 머리와 본문은 그대로 표시한다.

- 채팅 머리를 포탈 스킨의 얼굴·모자 이미지로 생성해 기본 채팅에 표시한다. 자기 스킨을 찾지 못하던 WASM 캐시 패치를 제거했다.
- 브라우저 먼 지형 LOD를 추가한다. 이미 로드된 청크의 4블록 간격 표면만 읽고 별도 Worker에서 메시를 만들며, 기기별 256~512청크를 저장한다. 서버 청크 생성은 요청하지 않는다. 같은 장면의 20초 켜기·끄기 비교에서 약 60 FPS와 50ms 초과 프레임 간격 0회를 확인했다. 이동·새 청크 생성·다른 기기는 별도 검증이 필요하다.

- 링크 미리보기 이미지의 영문 글자도 갈무리11로 바꾸고 기본 Minecraft ASCII 폰트 추출을 없앴다.

- 포탈·로딩 화면·게임의 한글, 영문, 숫자를 갈무리11로 통일한다. 리소스 메타데이터에 한국어를 등록해 저장된 한국어 설정이 실제로 적용되게 한다.
- 게임 iframe 포커스를 자동으로 옮겨 키 입력이 포탈에 남는 문제를 고쳤다. 기본 Esc 나가기 버튼을 포탈 복귀로 연결하고 중복 HTML 복귀 버튼을 제거한다.
- 해시로 고정한 26.2 WASM의 화면 전환을 포탈에 연결하고 시작 프로필 래퍼를 건너뛴다. 기본 로케이터를 유지하고 중복 HUD를 제거한다.
- 기본 텍스처로 통일하고, 서버 시작 중 클라이언트 다운로드·컴파일을 진행한다. 멀티플레이에서는 내장 서버 worker WASM 다운로드·컴파일을 생략한다.
- 횃불 셰이더 uniform 값이 바뀔 때만 GPU에 전송한다. 빈 서버 자동 종료 기본값과 허용 최소값을 조정해 3분 종료를 지원한다.
- 변경 내역은 날짜별로 짧은 기록만 표시한다. 상세 펼치기와 항목별 상자를 없애고 날짜는 회색으로 표시한다. 기록 사이 간격은 flex gap으로 관리한다.
- 로컬 실제 26.2 접속에서 한국어 메뉴, 영문·숫자·한글 갈무리11, 키 입력, 기본 메뉴에서 포탈 복귀를 확인했다. 채팅 머리는 표시되지만 자신의 포탈 스킨과 일치시키는 작업은 남아 있다. Voxy/Distant Horizons와 Fabric 렌더러 모드는 아직 이식하지 않았다.

## 2026-09-05

- 운영 26.2 서버와 웹 배포를 확인하고 실제 계정으로 새 스폰에 접속했다. 접속 후 저장된 인벤토리·장비·엔더 상자·경험치가 변환본과 같음을 검증했다. 최종 백업은 331개 파일, 변환 대상은 46명·아이템 5,783개이며 318개 테스트가 통과했다.
- 알려진 미완료 항목: 26.2의 첫 프로필 화면은 Done을 눌러야 하며, 채팅 머리 아이콘과 일부 게임 내부 UI의 기존 버전과 동일한 이식은 아직 남아 있다.

- 기존 운영 서버의 인원 제한·난이도·게임 모드 등 설정도 26.2로 보존하고, 현대 버전의 난이도·게임 모드를 문자열 형식으로 저장하도록 했다.

- 26.2 실행 옵션에도 한국어를 지정해 저장된 언어 설정을 기본 영어가 덮어쓰지 않도록 했다.
- 운영 런타임을 Java 25 기반 Paper 26.2와 인증된 브라우저 게이트웨이로 전환한다. 최신 운영 백업의 46명 인벤토리·장비·엔더 상자·경험치를 변환하고 새 지형의 스폰으로 옮긴다. 기존 월드와 계정 DB는 보존한다.
- 26.2 클라이언트에 기존 한글 폰트와 호환 텍스처, 자동 GUI 크기, 모바일 메뉴 키, 포탈 복귀 버튼을 연결했다. 횃불 밝기는 브라우저 셰이더에서 계산하며 서버 블록 조명을 변경하지 않는다.

- 아이콘 편집기에서 확정한 Eye와 Calendar 픽셀 모양을 앱 원본에 반영했다.

- 아이콘 편집기에서 Eye와 Calendar를 눌렀을 때 편집 격자가 열리지 않던 오류를 고쳤다.

- 20:00 기준 운영 1.12.2 서버의 월드 3개, 플레이어 데이터, 설정, 계정 DB와 스킨을 로컬 Downloads에 백업했다. 압축 파일 SHA-256과 서버 파일 330개의 해시를 검증했다.
- Java 26.2 이식 작업을 main 작업 폴더로 합쳤다. 기존 미커밋 UI 변경은 보존했다.
- 첫 접속에 `26.2로 옮기는중!!` 안내와 재시작·게임 종료 가능성 설명을 표시하고, `그래도 들어가기`를 누른 뒤 기존 포탈을 불러오도록 했다.
- 운영 Minecraft 런타임의 26.2 전환과 전체 게임 개조 기능 검증은 진행 중이다.
- 26.2 플러그인에 기존 비공개 관전 처리 코드를 옮기고, 채팅 입력을 제외한 공통 HUD·모바일 조작 코드를 생성하는 어댑터를 추가했다. 컴파일·생성은 확인했으며 실제 26.2 게임 검증은 아직 완료하지 않았다.

- 관전 버튼의 가운데 눈알을 위아래 눈꺼풀에 닿도록 늘렸다.

- 관전 버튼의 눈을 기존 9×9 픽셀 아이콘 방식으로 바꾸고 회색을 유지했다.

- OP 계정용 회색 눈 버튼과 서명된 관전 접속 모드를 추가했다.
- 관전 캐릭터를 기존 플레이 캐릭터와 분리하고 관전자 모드로 고정했다.
- 관전자의 접속·퇴장 메시지, 탭 목록, 공개 접속 목록, /list와 서버 조회 인원을 숨겼다.
- 관전 중 채팅, 명령어, 상호작용을 차단했다.
- 이 항목은 공개 업데이트 창이나 배포 알림에 추가하지 않는다.
- 관전 기능은 커밋 `32a1af0`으로 운영 웹·서버 배포를 완료했다.
- 자동 테스트와 임시 Paper/Eagler 서버의 접속 패킷 검증을 수행했다. 기존 연결 유지, 관전자 모드, 기존 및 신규 접속자에게 관전 정보 미전달, 채팅 차단을 확인했다.
- 브라우저에서 실제 관전 조작과 장시간 서버 부하는 아직 검증하지 않았다.
